<%init>
use Data::Dumper;
my $ref = $c->stash->{records};

</%init>

% foreach my $rec ( keys %{$ref} ) {
    <% $rec %>
    <% $ref->{$rec}->{title} %>
    <% $ref->{$rec}->{author} %>
    <% $ref->{$rec}->{pubdate} %>
    <% $ref->{$rec}->{reldate} %>
    <br/>
% }
