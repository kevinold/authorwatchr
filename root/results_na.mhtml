<%init>
my $response = $c->stash->{records};

</%init>
% if ($response) {
%   for my $prop ($response->properties) {
%       next if $prop->year() > 2008;
        <% $prop->as_string() %> <b><% $prop->ReleaseDate() %></b> <% $prop->similar_asins() %> 
        <br/>
        <% $prop->ProductDescription() %>
        <% $prop->DetailPageURL() %>
        <br/>
        <br/>
%   } 
% }
